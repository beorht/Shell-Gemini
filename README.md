# 🚀 ShellGemini

> Минималистичный bash-скрипт для взаимодействия с Google Gemini API из командной строки

ShellGemini - это легковесная CLI утилита, которая позволяет общаться с AI моделью Google Gemini прямо из терминала. Идеально подходит для быстрых вопросов, генерации кода, объяснения команд и решения задач без необходимости открывать браузер.

---

## ✨ Возможности

- 🎯 **Простота** - один скрипт, минимум зависимостей
- ⚡ **Быстро** - получайте ответы за секунды
- 🎨 **Красиво** - поддержка цветного вывода и markdown рендеринга
- 🔒 **Безопасно** - API ключ в `.env`, не хардкодится в скрипте
- 🛠️ **Настраиваемо** - системные промпты для разных задач
- 📝 **Markdown** - ответы с подсветкой кода

---

## 📦 Установка

### Шаг 1: Клонировать репозиторий

```bash
git clone https://github.com/your-username/shellgemini.git
cd shellgemini
```

### Шаг 2: Установить зависимости

**На Manjaro/Arch Linux:**
```bash
sudo pacman -S curl jq
```

**На Ubuntu/Debian:**
```bash
sudo apt install curl jq
```

**На macOS:**
```bash
brew install curl jq
```

### Шаг 3: Настроить API ключ

1. Получите бесплатный API ключ в [Google AI Studio](https://aistudio.google.com/app/apikey)
2. Скопируйте шаблон конфигурации:
   ```bash
   cp .env.example .env
   ```
3. Откройте `.env` и добавьте ваш API ключ:
   ```bash
   nano .env
   # или
   vim .env
   ```
   ```bash
   GEMINI_API=ваш_api_ключ_здесь
   ```

### Шаг 4: Сделать скрипт исполняемым

```bash
chmod +x shell-gm.sh
```

### Шаг 5 (опционально): Добавить в PATH

Чтобы запускать из любой директории:

```bash
# Создайте символическую ссылку в /usr/local/bin
sudo ln -s "$(pwd)/shell-gm.sh" /usr/local/bin/shell-gm

# Теперь можно использовать просто:
shell-gm "ваш вопрос"
```

---

## 🎯 Использование

### Базовое использование

```bash
./shell-gm.sh "ваш вопрос к Gemini"
```

### Примеры

**Простой вопрос:**
```bash
./shell-gm.sh "Что такое Linux?"
```

**Генерация кода:**
```bash
./shell-gm.sh "Напиши bash скрипт для создания бэкапа директории /home"
```

**Объяснение команды:**
```bash
./shell-gm.sh "Объясни что делает: find . -name '*.log' -mtime +7 -delete"
```

**Отладка ошибок:**
```bash
./shell-gm.sh "Почему команда 'rm -rf /' опасна?"
```

**Помощь с Git:**
```bash
./shell-gm.sh "Как откатить последний коммит в Git?"
```

**Конвертация между форматами:**
```bash
./shell-gm.sh "Преобразуй JSON в YAML: {\"name\": \"test\", \"value\": 123}"
```

### Справка

```bash
./shell-gm.sh --help
# или
./shell-gm.sh -h
```

---

## ⚙️ Конфигурация

### Файл `.env`

```bash
# API ключ Google Gemini (обязательно)
GEMINI_API=your_api_key_here

# Системный промпт (опционально)
SYSTEM_PROMPT="Ты полезный ассистент командной строки. Давай краткие и точные ответы."
```

### Системные промпты

Вы можете настроить поведение AI, изменив `SYSTEM_PROMPT` в `.env`:

**Для программирования:**
```bash
SYSTEM_PROMPT="Ты эксперт-программист. Давай готовые решения с рабочим кодом. Объясняй кратко. Используй best practices. Всегда добавляй комментарии."
```

**Для DevOps:**
```bash
SYSTEM_PROMPT="Ты DevOps инженер. Давай готовые команды и скрипты для production. Приоритет - безопасность и надежность. Всегда предупреждай об опасных операциях."
```

**Для обучения:**
```bash
SYSTEM_PROMPT="Ты терпеливый учитель. Объясняй простым языком с примерами. Разбивай сложные концепции на простые шаги."
```

---

## 🎨 Улучшение вывода

### Установка Glow (рекомендуется)

Для красивого рендеринга markdown ответов:

```bash
# Arch/Manjaro
sudo pacman -S glow

# Ubuntu/Debian (добавьте репозиторий)
sudo mkdir -p /etc/apt/keyrings
wget -qO- https://repo.charm.sh/apt/gpg.key | sudo gpg --dearmor -o /etc/apt/keyrings/charm.gpg
echo "deb [signed-by=/etc/apt/keyrings/charm.gpg] https://repo.charm.sh/apt/ * *" | sudo tee /etc/apt/sources.list.d/charm.list
sudo apt update && sudo apt install glow

# macOS
brew install glow
```

После установки `glow` скрипт автоматически будет использовать его для красивого вывода.

### Альтернативы

- **bat** - подсветка синтаксиса (без рендеринга markdown)
- **mdcat** - легковесный markdown рендерер
- **rich-cli** - продвинутое форматирование (Python)

---

## 📂 Структура проекта

```
shellgemini/
├── shell-gm.sh          # Основной исполняемый скрипт
├── .env                 # Конфигурация (API ключ, промпты) - НЕ КОММИТИТЬ!
├── .env.example         # Шаблон для .env
├── .gitignore           # Игнорируемые файлы (включая .env)
├── README.md            # Документация (этот файл)
├── TODO.md              # Планируемые улучшения
└── CLAUDE.md            # Инструкции для Claude Code AI
```

---

## 🔧 Troubleshooting

### Ошибка: "jq не установлен"

**Решение:**
```bash
# Arch/Manjaro
sudo pacman -S jq

# Ubuntu/Debian
sudo apt install jq

# macOS
brew install jq
```

---

### Ошибка: "Файл .env не найден"

**Решение:**
```bash
cp .env.example .env
nano .env  # добавьте ваш API ключ
```

---

### Ошибка: "API вернул ошибку (HTTP 400)"

**Возможные причины:**
- Неправильный API ключ
- Истек срок действия ключа

**Решение:**
1. Проверьте правильность ключа в `.env`
2. Создайте новый ключ в [Google AI Studio](https://aistudio.google.com/app/apikey)

---

### Ошибка: "Quota exceeded" (HTTP 429)

**Причина:** Превышен лимит бесплатных запросов

**Бесплатные лимиты Gemini 1.5 Flash:**
- 15 запросов в минуту
- 1,500 запросов в день
- 1 миллион токенов в день

**Решения:**
1. Подождите несколько минут
2. Проверьте квоту: https://ai.dev/usage?tab=rate-limit
3. Перейдите на платный план в Google Cloud
4. Используйте несколько API ключей (ротация)

---

### Ошибка: "syntax error: unexpected end of file"

**Причина:** Проблема с heredoc (EOF имеет отступы)

**Решение:** Убедитесь, что `EOF` в строке 31 не имеет пробелов в начале:
```bash
cat << EOF
текст
EOF
```
А не:
```bash
cat << EOF
текст
  EOF  # ❌ Неправильно
```

---

### Пустой ответ или ошибка парсинга

**Причина:** Опечатка в переменной `$PROPT` (должно быть `$PROMPT`)

**Решение:** Проверьте строку 61 в `shell-gm.sh`:
```bash
ESCAPED_PROMPT=$(echo "$PROMPT" | jq -Rs .)  # ✅ Правильно
# НЕ:
ESCAPED_PROMPT=$(echo "$PROPT" | jq -Rs .)   # ❌ Опечатка
```

---

## 🔒 Безопасность

### ⚠️ ВАЖНО: Защита API ключа

1. **Никогда не коммитьте `.env` в git**
   - Файл `.gitignore` уже настроен для этого
   - Проверьте: `git status` не должен показывать `.env`

2. **Проверьте права доступа к `.env`:**
   ```bash
   chmod 600 .env
   ls -la .env
   # Должно быть: -rw------- (только владелец может читать/писать)
   ```

3. **Ротация ключей:**
   - Периодически создавайте новые ключи
   - Удаляйте старые в [Google AI Studio](https://aistudio.google.com/app/apikey)

4. **Если ключ скомпрометирован:**
   ```bash
   # 1. Немедленно удалите ключ в Google AI Studio
   # 2. Создайте новый ключ
   # 3. Обновите .env
   # 4. Проверьте историю git:
   git log -p .env  # НЕ должно быть в истории!
   ```

---

## 🚀 Продвинутое использование

### Использование в скриптах

```bash
#!/bin/bash

# Получить ответ и сохранить в переменную
ANSWER=$(./shell-gm.sh "Что такое Docker?" 2>/dev/null | tail -n +2)

echo "Gemini ответил: $ANSWER"
```

### Pipe с другими командами

```bash
# Объяснить вывод команды
ls -la | ./shell-gm.sh "Объясни этот вывод ls"

# Исправить код
cat script.sh | ./shell-gm.sh "Найди баги в этом скрипте"
```

### Создание алиасов

Добавьте в `~/.bashrc` или `~/.zshrc`:

```bash
alias ask='~/Projects/shellgemini/shell-gm.sh'
alias explain='~/Projects/shellgemini/shell-gm.sh "Объясни команду: "'
alias code='~/Projects/shellgemini/shell-gm.sh "Напиши код для: "'
alias fix='~/Projects/shellgemini/shell-gm.sh "Исправь ошибку: "'
```

Теперь можно использовать:
```bash
ask "Что такое Kubernetes?"
explain "docker-compose up -d"
code "парсинг JSON в Python"
fix "TypeError: unsupported operand type"
```

---

## 📊 Ограничения API

### Бесплатный tier (Gemini 1.5 Flash)

| Параметр | Лимит |
|----------|-------|
| Запросов в минуту | 15 |
| Запросов в день | 1,500 |
| Токенов в день | 1,000,000 |
| Длина запроса | 32,000 токенов |
| Длина ответа | 8,192 токена |

### Какую модель использует скрипт?

По умолчанию: `gemini-1.5-flash` (быстрая, бесплатная, стабильная)

Другие доступные модели:
- `gemini-1.5-pro` - более умная, но медленнее
- `gemini-2.0-flash-exp` - экспериментальная (низкие лимиты!)

Чтобы сменить модель, отредактируйте строку 67 в `shell-gm.sh`:
```bash
"https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent"
#                                                          ^^^^^^^^^^^^^
#                                                          Название модели
```

---

## 🤝 Вклад в проект

Хотите помочь улучшить ShellGemini?

1. Fork репозитория
2. Создайте feature ветку: `git checkout -b feature/amazing-feature`
3. Сделайте изменения и коммит: `git commit -m 'Add amazing feature'`
4. Push в ветку: `git push origin feature/amazing-feature`
5. Откройте Pull Request

### Идеи для вклада

Смотрите [TODO.md](TODO.md) для списка планируемых улучшений.

---

## 📜 Лицензия

MIT License - свободное использование для любых целей.

---

## 🔗 Полезные ссылки

- [Google AI Studio](https://aistudio.google.com/) - получить API ключ
- [Gemini API Documentation](https://ai.google.dev/gemini-api/docs) - документация API
- [Проверка квоты](https://ai.dev/usage?tab=rate-limit) - мониторинг использования
- [Glow](https://github.com/charmbracelet/glow) - markdown рендерер

---

## ❓ FAQ

**Q: Это бесплатно?**
A: Да! Google Gemini API имеет щедрый бесплатный tier (1500 запросов/день).

**Q: Работает без интернета?**
A: Нет, требуется подключение к API. Но можно использовать локальные модели через Ollama (планируется поддержка).

**Q: Поддерживает ли мультимодальность (изображения)?**
A: Пока нет, но это в планах (см. TODO.md).

**Q: Сохраняется ли история запросов?**
A: Пока нет, каждый запрос независимый. История и chat-режим в планах.

**Q: Можно ли использовать другие LLM (ChatGPT, Claude)?**
A: Пока только Gemini, но поддержка других API в планах.

**Q: Безопасно ли отправлять конфиденциальные данные?**
A: Читайте [Privacy Policy Google](https://policies.google.com/privacy). Не отправляйте секреты, пароли, персональные данные.

---

## 💬 Поддержка

Если у вас проблемы или вопросы:

1. Проверьте раздел [Troubleshooting](#-troubleshooting)
2. Посмотрите [открытые issues](https://github.com/your-username/shellgemini/issues)
3. Создайте новый issue с описанием проблемы

---

**Сделано с ❤️ для Linux сообщества**

*Последнее обновление: 2025-12-24*
